<div class="comments">
  <ul>
    <% comments.each do |comment|%>
      <%= erb :'partials/_single_comment', locals: {comment: comment} %>
    <%end%>
  </ul>
</div>
<%=erb :'partials/_comment_link'%>
